<?php
/**
 * /etc/rem/env.php
 * Configuracion de produccion del Sistema REM.
 *
 * Permisos: 640, owner: root:www-data.
 *
 * Generar el archivo copiando deploy/env.production.example y
 * rellenando con valores reales. NUNCA commitear.
 *
 * Este archivo vive fuera del web root y es cargado por config/config.php
 * cuando se define REM_ENV_FILE en el VirtualHost de Apache.
 */

return [
    // Base de datos
    'host'    => 'localhost',
    'port'    => 3306,
    'name'    => 'observaciones_rem',
    'user'    => 'rem_app',
    'pass'    => 'CAMBIAR_POR_PASSWORD_GENERADA',
    'charset' => 'utf8mb4',

    // SMTP para reset de contrasena por email
    'smtp' => [
        'host'     => 'smtp.example.cl',
        'port'     => 587,
        'user'     => 'rem@example.cl',
        'pass'     => 'CAMBIAR_POR_PASSWORD_SMTP',
        'from'     => 'no-reply@rem.example.cl',
        'from_name' => 'Sistema de Observaciones REM',
        'secure'   => 'tls',
    ],
];
